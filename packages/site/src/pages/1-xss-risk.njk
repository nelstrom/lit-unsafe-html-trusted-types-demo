---
layout: layout.njk
title: "1. XSS Risk"
---

<article>
  <span class="badge badge-danger">XSS RISK</span>
  <h2>1. XSS Risk</h2>

  <div class="csp-indicator csp-off">● Trusted Types: OFF</div>

  <div class="outcome-grid">
    <span class="outcome-label">Expected:</span>
    <span>Alert fires — <code>unsafeHTML()</code> passes raw HTML straight to <code>innerHTML</code>.</span>
    <span class="outcome-label">What actually happens:</span>
    <span class="outcome-match">✓ Alert fires.</span>
  </div>

  <div class="code-block">
    <div class="code-block-label">demo-1.js</div>
{% highlight "javascript" %}
import { html, render } from 'lit-html';
import { unsafeHTML } from 'lit-html/directives/unsafe-html.js';

document.getElementById('run-btn').addEventListener('click', () => {
  const userInput = document.getElementById('user-input').value;
  const result = document.getElementById('result');
  result.textContent = '';
  const litTarget = document.createElement('div');
  result.appendChild(litTarget);
  render(html`<div>${unsafeHTML(userInput)}</div>`, litTarget);
});
{% endhighlight %}
  </div>

  <div class="demo-area">
    <label for="user-input">HTML input:</label>
    <textarea id="user-input" rows="3"><img src=x onerror="alert('XSS!')"> Hello!</textarea>
    <button class="run-btn" id="run-btn">Run Demo</button>
    <div id="result"></div>
  </div>

  <div class="page-nav">
    <span></span>
    <a href="{{ '/pages/2-manual-sanitization/' | url }}">Next: DOMPurify →</a>
  </div>
</article>

<script type="module" src="{{ '/js/demo-1.js' | url }}"></script>
