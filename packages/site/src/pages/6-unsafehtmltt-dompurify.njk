---
layout: layout.njk
title: "6. TT: sanitize"
---

<article>
  <span class="badge badge-warning">NO ENFORCEMENT</span>
  <h2>6. unsafeHtmlTT — DOMPurify String</h2>

  <div class="csp-indicator csp-off">● Trusted Types: OFF</div>

  <div class="outcome-grid">
    <span class="outcome-label">Expected:</span>
    <span>No XSS — DOMPurify sanitizes, <code>unsafeHtmlTT()</code> renders the clean string.</span>
    <span class="outcome-label">What actually happens:</span>
    <span class="outcome-match">✓ No alert — renders sanitized string.</span>
  </div>

  <div class="code-block">
    <div class="code-block-label">demo-6.js</div>
{% highlight "javascript" %}
import { html, render } from 'lit-html';
import { unsafeHtmlTT } from 'lit-html/directives/unsafe-html-tt.js';
import DOMPurify from 'dompurify';

document.getElementById('run-btn').addEventListener('click', () => {
  const userInput = document.getElementById('user-input').value;
  const sanitized = DOMPurify.sanitize(userInput);
  const result = document.getElementById('result');
  result.textContent = '';
  const litTarget = document.createElement('div');
  result.appendChild(litTarget);
  render(html`<div>${unsafeHtmlTT(sanitized)}</div>`, litTarget);
});
{% endhighlight %}
  </div>

  <div class="demo-area">
    <label for="user-input">HTML input:</label>
    <textarea id="user-input" rows="3"><img src=x onerror="alert('XSS!')"> Hello!</textarea>
    <button class="run-btn" id="run-btn">Run Demo</button>
    <div id="result"></div>
  </div>

  <div class="page-nav">
    <a href="{{ '/pages/5-unsafehtmltt-compat/' | url }}">← TT: compat</a>
    <a href="{{ '/pages/7-unsafehtmltt-tt-string/' | url }}">Next: TT: enforced →</a>
  </div>
</article>

<script type="module" src="{{ '/js/demo-6.js' | url }}"></script>
